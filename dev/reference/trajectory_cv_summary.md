# Bin Trajectory Time Values

Internal helper used by trajectory analyses to make nominally identical
sliding-window time points match exactly across renditions.

## Usage

``` r
trajectory_cv_summary(summary_df, labs_order)
```
