# Fast PCA Using IRLBA

Internal function for fast PCA using truncated SVD.

## Usage

``` r
fast_pca(matrix, n_components = 50, center = TRUE, scale = TRUE)
```

## Arguments

- matrix:

  Input matrix

- n_components:

  Number of components

- center:

  Whether to center data

- scale:

  Whether to scale data
