# Compute RMS Envelope

Internal function to calculate RMS envelope of audio signal.

## Usage

``` r
compute_rms(data, wl, ovlp)
```

## Arguments

- wl:

  Window length for RMS calculation (default: 1024)

- ovlp:

  Overlap percentage between windows (default: 50)
