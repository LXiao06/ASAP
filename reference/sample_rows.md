# Sample Rows from a Vector

An internal helper function to sample a specified number of rows from a
given vector of row indices.

## Usage

``` r
sample_rows(rows, n)
```

## Arguments

- rows:

  A vector of row indices

- n:

  Number of rows to sample

## Value

A vector of sampled row indices
