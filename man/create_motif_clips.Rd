% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif_clips.R
\name{create_motif_clips}
\alias{create_motif_clips}
\alias{create_motif_clips.default}
\alias{create_motif_clips.Sap}
\title{Create Motif Audio Clips}
\usage{
create_motif_clips(x, ...)

\method{create_motif_clips}{default}(
  x,
  wav_dir,
  indices = NULL,
  n_motifs = NULL,
  seed = 222,
  output_format = c("wav", "hdf5"),
  output_dir = NULL,
  hdf5_filename = "motifs.h5",
  metadata_filename = "metadata.csv",
  name_prefix = "motif",
  overwrite = TRUE,
  write_metadata = TRUE,
  verbose = TRUE,
  ...
)

\method{create_motif_clips}{Sap}(
  x,
  indices = NULL,
  n_motifs = NULL,
  seed = 222,
  output_format = c("wav", "hdf5"),
  output_dir = NULL,
  hdf5_filename = "motifs.h5",
  metadata_filename = "metadata.csv",
  name_prefix = "motif",
  overwrite = TRUE,
  write_metadata = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A SAP object or a motif data frame.}

\item{...}{Additional arguments passed to methods.}

\item{wav_dir}{For data-frame method: base directory containing source WAV files.}

\item{indices}{Optional numeric vector of motif row indices to export.
If NULL, all motifs are exported.}

\item{n_motifs}{Optional integer. Number of motifs to randomly sample when
\code{indices} is NULL. Sampling is applied per day/subdirectory
(\code{day_post_hatch}) when available. If NULL, all motifs are processed.}

\item{seed}{Integer random seed used when \code{n_motifs} sampling
is applied. In this repository, sampling is fixed to \code{222}.}

\item{output_format}{Output type: \code{"wav"} or \code{"hdf5"}.}

\item{output_dir}{Directory where output files are written.}

\item{hdf5_filename}{File name used when \code{output_format = "hdf5"}.}

\item{metadata_filename}{Name of metadata CSV written to \code{output_dir}.}

\item{name_prefix}{Prefix used for generated motif clip names.}

\item{overwrite}{Logical. Overwrite existing output file(s) when TRUE.
Default is TRUE.}

\item{write_metadata}{Logical. Write metadata CSV when TRUE.}

\item{verbose}{Logical. Print one export summary per day and overall totals.}
}
\value{
For SAP input: updated SAP object with export summary in
\code{x$misc$motif_clip_exports}. For data-frame input: metadata data frame.
}
\description{
Exports motif-level clips detected by \code{find_motif()} either as WAV files
in a directory tree or as datasets in a single HDF5 file.
}
\details{
Output layout for \code{output_format = "wav"}:
\preformatted{
output_dir/motifs/{bird_id}/{day_post_hatch}/{name_prefix}_001.wav
}

Output layout for \code{output_format = "hdf5"}:
\preformatted{
output_dir/{hdf5_filename}
  /{bird_id}/{day_post_hatch}/{name_prefix}_001
}

If \code{bird_id} is not available in motif rows, the function attempts to
infer it from metadata. Missing values are stored as \code{"unknown_bird"}.
}
\examples{
\dontrun{
sap <- sap |>
  find_motif(template_name = "syllable_d", pre_time = 0.7, lag_time = 0.5) |>
  create_motif_clips(indices = 1:50, output_format = "wav")

sap <- create_motif_clips(
  sap,
  output_format = "hdf5",
  hdf5_filename = "motifs.h5"
)
}

}
