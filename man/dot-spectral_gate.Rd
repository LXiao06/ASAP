% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/denoise.R
\name{.spectral_gate}
\alias{.spectral_gate}
\title{Spectral Gating (soft sigmoid gate with configurable floor)}
\usage{
.spectral_gate(
  mag,
  noise_quantile = 0.25,
  threshold = 1.5,
  smoothing = 3L,
  floor = 0.1
)
}
\arguments{
\item{mag}{Numeric matrix (freq x time) — magnitude spectrogram.}

\item{noise_quantile}{Quantile for noise-floor estimation.}

\item{threshold}{Number of \eqn{\sigma} above noise floor where gate reaches
50 \\% transmission (default: 1.5).}

\item{smoothing}{Half-width (bins) of frequency-axis box-car mask smoother;
0 = disabled (default: 3).}

\item{floor}{Minimum gate value in [0, 1).}
}
\value{
Cleaned magnitude matrix.
}
\description{
Builds a per-frequency noise profile (floor \eqn{\mu_f} and spread
\eqn{\sigma_f}) then computes a gate value for every time-frequency bin:
\deqn{G(t,f) = \max\!\left(g_{\min},\;
  \frac{1}{1 + e^{-(|S(t,f)| - \mu_f - \theta\sigma_f)\,/\,(\sigma_f/4)}}
\right)}
The floor \eqn{g_{\min}} (\code{floor}) prevents complete silencing and
is the key parameter for preserving tonal continuity of bird song.
}
\keyword{internal}
