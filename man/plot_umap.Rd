% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap.R
\name{plot_umap}
\alias{plot_umap}
\alias{plot_umap.default}
\alias{plot_umap.Sap}
\title{Plot UMAP Visualization}
\usage{
plot_umap(x, ...)

\method{plot_umap}{default}(
  x,
  dims = c("UMAP1", "UMAP2"),
  subset.by = NULL,
  subset.value = NULL,
  group.by = NULL,
  split.by = NULL,
  cols = NULL,
  pt.size = 0.5,
  stroke = 0.5,
  alpha = 0.3,
  highlight.alpha = NULL,
  label = FALSE,
  label.size = 4,
  repel = FALSE,
  highlight.by = NULL,
  highlight.value = NULL,
  cols.highlight = "#DE2D26",
  sizes.highlight = 1,
  background.value = NULL,
  na.value = "grey80",
  ncol = NULL,
  combine = TRUE,
  ...
)

\method{plot_umap}{Sap}(
  x,
  segment_type = c("motifs", "syllables", "bouts", "segments"),
  dims = c("UMAP1", "UMAP2"),
  group.by = NULL,
  split.by = NULL,
  subset.by = NULL,
  subset.value = NULL,
  cols = NULL,
  pt.size = 0.5,
  stroke = 0.5,
  alpha = 0.3,
  highlight.alpha = NULL,
  label = FALSE,
  label.size = 4,
  repel = FALSE,
  highlight.by = NULL,
  highlight.value = NULL,
  cols.highlight = "#DE2D26",
  sizes.highlight = 1,
  background.value = NULL,
  na.value = "grey80",
  ncol = NULL,
  combine = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{An object to visualize, either a data frame with UMAP coordinates or a SAP object}

\item{...}{Additional arguments passed to specific methods}

\item{dims}{UMAP dimensions to plot (default: c("UMAP1", "UMAP2"))}

\item{subset.by}{Column name for subsetting data}

\item{subset.value}{Values to subset by}

\item{group.by}{Column name for grouping points}

\item{split.by}{Column name for faceting plots}

\item{cols}{Custom colors for groups}

\item{pt.size}{Point size (default: 0.5)}

\item{stroke}{Point stroke width (default: 0.5)}

\item{alpha}{Point transparency (default: 0.3)}

\item{highlight.alpha}{Transparency for highlighted points}

\item{label}{Whether to add labels (default: FALSE)}

\item{label.size}{Size of labels (default: 4)}

\item{repel}{Whether to use repelling labels (default: FALSE)}

\item{highlight.by}{Column name for highlighting}

\item{highlight.value}{Values to highlight}

\item{cols.highlight}{Colors for highlighted points (default: '#DE2D26')}

\item{sizes.highlight}{Size for highlighted points (default: 1)}

\item{background.value}{Background group value}

\item{na.value}{Color for NA values (default: 'grey80')}

\item{ncol}{Number of columns in multi-plot layout}

\item{combine}{Whether to combine multiple plots (default: TRUE)}

\item{segment_type}{For SAP objects: Type of segments to visualize ('motifs', 'syllables', 'bouts', 'segments')}

\item{verbose}{For SAP objects: Whether to print progress messages}
}
\value{
For default method: A ggplot object or list of plots
For SAP objects: Updated SAP object with plot as side effect
}
\description{
Creates customizable UMAP visualizations with options for grouping, highlighting, and customization.
}
\details{
This function creates UMAP visualizations with the following features:
\itemize{
\item Flexible grouping and highlighting
\item Customizable point appearance
\item Optional labels and faceting
\item Multiple plot combinations
}
}
\examples{
\dontrun{
# Basic UMAP plot from data frame
plot_umap(umap_df, group.by = "cluster")

# Plot with highlighting
plot_umap(umap_df,
          group.by = "cluster",
          highlight.by = "label",
          highlight.value = "a")

# Plot with faceting
plot_umap(umap_df,
          group.by = "cluster",
          split.by = "day_post_hatch")

# Plot from SAP object
plot_umap(sap_obj,
          segment_type = "motifs",
          group.by = "label")

# SAP object plot with custom grouping and highlighting
plot_umap(sap_obj,
          segment_type = "syllables",
          group.by = "label",
          highlight.by = "cluster",
          highlight.value = c(1, 2))
}

}
