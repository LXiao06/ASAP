% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duration.R
\name{plot_motif_boundaries}
\alias{plot_motif_boundaries}
\title{Visualize Motif Boundaries in Audio Data}
\usage{
plot_motif_boundaries(
  x,
  sample_percent = NULL,
  balanced = FALSE,
  labels = NULL,
  clusters = NULL,
  ordered = FALSE,
  descending = TRUE,
  cores = NULL,
  seed = 222,
  msmooth = c(256, 50),
  color_palette = NULL,
  n_colors = 500,
  contrast = 3,
  marginal_window = 0.1,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A SAP object containing motifs and metadata}

\item{sample_percent}{Percentage of data to sample from each group (0-100)}

\item{balanced}{Logical indicating whether to balance group sizes}

\item{labels}{Character vector of specific labels to include}

\item{clusters}{Numeric vector of cluster IDs to filter}

\item{ordered}{Logical indicating UMAP-based ordering}

\item{descending}{Logical for descending order in UMAP sorting}

\item{cores}{Number of processing cores (NULL for auto-detection)}

\item{seed}{Random seed for reproducibility}

\item{msmooth}{Vector of smoothing parameters for amplitude envelopes}

\item{color_palette}{Color palette function for heatmap}

\item{n_colors}{Number of color gradations in palette}

\item{contrast}{Contrast adjustment for color scaling}

\item{marginal_window}{Time window extension for motif margins (seconds)}

\item{verbose}{Logical flag for progress messages}

\item{...}{Additional parameters passed to lattice::levelplot}
}
\value{
A lattice plot object showing motif boundaries heatmap
}
\description{
Generates a heatmap visualization of motif boundaries with optional cluster filtering
and UMAP-based ordering. Incorporates amplitude envelopes and boundary markers for
precise temporal analysis.
}
\details{
Key features:
\itemize{
\item Integrates motif boundary data from refine_motif_boundaries()
\item Supports cluster filtering using feature embeddings
\item Enables UMAP-based ordering of motifs
\item Visualizes boundaries with colored markers (green = onset, cyan = offset)
\item Includes automatic duration calculation and marginal window extension
}

Requires prior execution of refine_motif_boundaries() for boundary detection.
Uses parallel processing for efficient amplitude envelope calculation.
}
\examples{
\dontrun{
# Basic usage with default parameters
plot_motif_boundaries(sap_object)

# Cluster-specific plot with custom colors
plot_motif_boundaries(sap_object,
                      clusters = c(1,3),
                      ordered = TRUE)
}

}
