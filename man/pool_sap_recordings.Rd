% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objects.R
\name{pool_sap_recordings}
\alias{pool_sap_recordings}
\title{Pool Selected Recording Folders into a Standard SAP Object}
\usage{
pool_sap_recordings(
  selections,
  output_dir,
  method = c("link", "copy"),
  overwrite = FALSE
)
}
\arguments{
\item{selections}{Data frame with character columns \code{animal_id}, \code{base_path},
\code{subfolder}, and \code{label}; one row per selected folder.}

\item{output_dir}{Directory for the pooled recording tree.}

\item{method}{Either \code{"link"} (default) to create symbolic links or \code{"copy"}
to copy WAV files.}

\item{overwrite}{Logical; replace existing files in the pooled tree.}
}
\value{
A regular SAP object. Its day folders are named
\verb{<animal_id>__<subfolder>} to keep source files distinct.
}
\description{
Links or copies selected recording folders into one conventional SAP folder
tree, then creates a regular SAP object. This lets the existing pipeline run
without multi-animal path handling.
}
\examples{
\dontrun{
sap <- pool_sap_recordings(data.frame(
  animal_id = c("bird_1", "bird_2"),
  base_path = c("/data/bird_1", "/data/bird_2"),
  subfolder = c("60", "65"),
  label = c("60 dph", "65 dph")
), output_dir = "/data/pooled_recordings")
}

}
