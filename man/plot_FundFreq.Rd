% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pitch.R
\name{plot_FundFreq}
\alias{plot_FundFreq}
\alias{plot_FundFreq.default}
\alias{plot_FundFreq.matrix}
\alias{plot_FundFreq.Sap}
\title{Plot Heatmap of Fundamental Frequency}
\usage{
plot_FundFreq(x, ...)

\method{plot_FundFreq}{default}(
  x,
  wav_dir = NULL,
  wl = 512,
  ovlp = 50,
  fmax = 1400,
  threshold = 10,
  plot_freq_lim = NULL,
  color_palette = NULL,
  n_colors = 500,
  cores = NULL,
  ...
)

\method{plot_FundFreq}{matrix}(
  x,
  labels = NULL,
  plot_freq_lim = NULL,
  color_palette = NULL,
  n_colors = 500,
  main = "Fundamental Frequency Heatmap",
  ...
)

\method{plot_FundFreq}{Sap}(
  x,
  segment_type = c("motifs", "syllables", "segments"),
  sample_percent = NULL,
  balanced = FALSE,
  labels = NULL,
  clusters = NULL,
  cores = NULL,
  seed = 222,
  wl = 512,
  ovlp = 50,
  fmax = 1400,
  threshold = 10,
  plot_freq_lim = NULL,
  color_palette = NULL,
  n_colors = 500,
  ordered = FALSE,
  descending = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{An object to visualize (data frame, SAP object, or matrix)}

\item{...}{Additional arguments passed to specific methods}

\item{wav_dir}{For default method: Path to WAV files directory}

\item{wl}{Window length for spectral analysis (default: 512)}

\item{ovlp}{Overlap percentage between windows (default: 50)}

\item{fmax}{Maximum frequency to consider (default: 1400 Hz)}

\item{threshold}{Threshold for pitch detection (default: 10)}

\item{plot_freq_lim}{Optional two-element vector to limit frequency range in plot}

\item{color_palette}{Function generating color palette}

\item{n_colors}{Number of colors in heatmap (default: 500)}

\item{cores}{Number of processing cores}

\item{labels}{Optional vector of labels to include}

\item{main}{For matrix method: Plot title}

\item{segment_type}{For SAP objects: Type of segments ('motifs')}

\item{sample_percent}{For SAP objects: Percentage to sample}

\item{balanced}{For SAP objects: Balance across labels}

\item{clusters}{Numeric vector of cluster IDs to filter}

\item{seed}{Random seed (default: 222)}

\item{ordered}{For SAP objects: Order by embeddings}

\item{descending}{For SAP objects: Direction of ordering}

\item{verbose}{Print progress messages}
}
\value{
For default method: List containing F0 matrix, reference time, and plot
For SAP objects: Updated object with fundamental frequency features
For matrices: Lattice plot object
}
\description{
Creates heatmap visualizations of fundamental frequency from audio segments, supporting
multiple data sources and visualization options.
}
\details{
Fundamental frequency estimation methods:
\itemize{
\item Uses cepstrum-based pitch detection
\item Normalizes time series across renditions
\item Supports optional frequency range limiting
}

For data frames:
\itemize{
\item Requires columns: filename, start_time, end_time
\item Extracts fundamental frequency for each segment
\item Creates matrix of aligned F0 values
}

For SAP objects:
\itemize{
\item Currently supports only 'motifs' segment type
\item Optional balanced sampling
\item Parallel processing support
\item Ordering by feature embeddings
}

For matrices:
\itemize{
\item Direct visualization of pre-computed F0 data
\item Label-based organization
\item Visual separation between groups
}
}
\examples{
\dontrun{
# Basic usage with data frame
plot_FundFreq(segments, wav_dir = "path/to/wavs")

# Limit frequency range
plot_FundFreq(segments,
              wav_dir = "path/to/wavs",
              plot_freq_lim = c(0.5, 1.5))

# SAP object with options
plot_FundFreq(sap_obj,
              ordered = TRUE,
              descending = FALSE)

# Matrix with specific labels and color palette
plot_FundFreq(f0_matrix,
              labels = c("a", "b"),
              color_palette = custom_palette)
}

}
\seealso{
\code{\link{FF}} for fundamental frequency estimation
}
